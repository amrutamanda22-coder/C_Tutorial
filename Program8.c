#include<stdio.h>
int main()
{
    int a,b,SUM;
    printf("Enter first number:");
    scanf("%d",&a);

    printf("Enter second number:");
    scanf("%d",&b);

    SUM = a + b;
    printf("Sum of two numbers is: %d",SUM);

    return 0;
}